# LLM 面试笔记维护规范

## 章节首页的 Fast Look

每个主章节的 `README.md` 必须在标题后提供 `## Fast Look`，用于让读者无需打开子文档即可快速定位知识点。

### 实体级要求

- 不要只写“Transformer”“微调”“RAG”等宽泛类别；必须拆成可独立查询的具体实体，例如 `MHA`、`GQA`、`RMSNorm`、`RoPE`、`SwiGLU`、`LoRA`、`QLoRA`、`PagedAttention`、`PPO`、`DPO`。
- 章节内出现的每个重要方法、算法、模型组件、框架、数据格式、训练/推理策略或评测方法，都应在 Fast Look 中有对应条目。
- 同类实体可以放在同一条目中比较，但必须分别说明它们的差异；不要用一长串名词替代解释。

### 每个实体的固定信息

每个实体应使用四个字段：

1. **描述**：它是什么、在系统或流程中做什么。
2. **与之前方法的区别**：它相对前代/基线改变了什么；没有直接前代时，说明它与相近方案的区别。
3. **优点**：带来的能力、效率、稳定性或工程收益。
4. **缺点/注意点**：代价、适用边界、常见误解、关键超参数或工程风险。

推荐模板：

```md
#### 实体名称（英文全称）

- **描述**：……
- **与之前方法的区别**：……
- **优点**：……
- **缺点/注意点**：……
```

### 组织与质量要求

- 在 Fast Look 开头给出一行章节级链路，说明实体之间的输入、输出或依赖关系，例如 `Tokenizer → RoPE → GQA → KV Cache → Decode`。
- 按读者理解顺序分组，例如“输入与表示 → 核心计算 → 训练/推理优化 → 模型/框架 → 实战”。
- Fast Look 是导航和对比索引，不复制子文章全文；描述保持简洁，但不能省略差异、优点和局限。
- 保留原有章节目录和文章链接；Fast Look 应位于目录之前。
- 新增、删除或重命名章节子文档时，同步更新对应 Fast Look；新增实体时优先补充实体条目而不是只追加名词。
- 修改后至少检查：所有本地链接存在、Markdown 格式无误、每个实体条目包含四个固定字段。
